Django==5.0.6
djangorestframework==3.15.1
psycopg[binary]>=3.2.2
django-cors-headers==4.3.1
django-environ==0.11.2
gunicorn==22.0.0
whitenoise==6.6.0
# Geospatial support comes from GeoDjango (built into Django) + PostGIS;
# GDAL/GEOS are system libraries installed via the Dockerfile/Render build.
